import {makeStyles} from '@material-ui/core/styles';

export const useStyles = makeStyles(() => ({
    input: {
        display: 'block',
        width: 400,
        marginTop: 10,
        marginBottom: 10,
    },
    submitBtn: {
        display: 'block',
        width: 200,
        marginTop: 10,
        marginBottom: 10,
    }
}));
