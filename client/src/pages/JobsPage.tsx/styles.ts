import {makeStyles} from '@material-ui/core/styles';

export const useStyles = makeStyles(() => ({
    jobsWrapper: {
        display: 'flex',
        flexDirection: 'row',
        flexWrap: 'wrap',
    }
}));
